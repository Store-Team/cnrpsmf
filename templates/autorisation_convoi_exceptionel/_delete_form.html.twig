<form method="post" action="{{ path('app_autorisation_convoi_exceptionel_delete', {'id': autorisation_convoi_exceptionel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autorisation_convoi_exceptionel.id) }}">
    <button class="btn">Delete</button>
</form>
