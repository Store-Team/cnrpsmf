<form method="post" action="{{ path('app_inspection_convoi_delete', {'matricule': inspection_convoi.matricule}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inspection_convoi.matricule) }}">
    <button class="btn">Delete</button>
</form>
