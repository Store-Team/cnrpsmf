<form method="post" action="{{ path('app_surveillance_chargement_delete', {'matricule': surveillance_chargement.matricule}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ surveillance_chargement.matricule) }}">
    <button class="btn">Delete</button>
</form>
